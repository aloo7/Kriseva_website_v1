---
// V6Nav.astro - UNUSED as of Gate 3 (docs/v2-handoffs/INDEPENDENT_REVIEW_V2.md
// Blocker 1). V6Layout.astro rendered this as a hardcoded, pre-v2,
// product-first nav disconnected from src/data/navigation.ts; it has been
// replaced there by the same Header.astro component BaseLayout uses.
// Left in place, not deleted, in case of rollback. Do not import.
//
// V6Nav.astro - fixed header for v6 inner pages.
// Desktop: mono route links + filled CTA. Mobile: MENU button opening a
// full-screen paper panel (keyboard reachable, aria-expanded wired).

interface Props {
  /** Current route path, e.g. '/evaluator' - sets aria-current. */
  current?: string;
}
const { current = '' } = Astro.props;

const links = [
  { href: '/tas', label: 'TAS' },
  { href: '/evaluator', label: 'Evaluator' },
  { href: '/capabilities', label: 'Capabilities' },
  { href: '/security', label: 'The Boundary' },
  { href: '/evidence', label: 'Evidence' },
  { href: '/founder', label: 'Founder' },
];
---
<a class="skip" href="#main">Skip to content</a>
<header class="v6" id="hdr">
  <div class="nav">
    <a class="brand" href="/" aria-label="KRISEVA home"><img class="bmark" src="/assets/brand/kriseva-mark.svg" alt="" width="24" height="24">KRISEVA</a>
    <nav class="nlinks" aria-label="Primary">
      {links.map((l) => (
        <a href={l.href} aria-current={current === l.href ? 'page' : undefined}>{l.label}</a>
      ))}
    </nav>
    <div style="display:flex;align-items:center;gap:12px">
      <a class="btn btn-fill btn-sm" href="/contact">Request a demo</a>
      <button class="menu-btn" id="menuBtn" aria-expanded="false" aria-controls="mobilePanel">Menu</button>
    </div>
  </div>
  <div class="mobile-panel" id="mobilePanel">
    <nav aria-label="Primary, mobile">
      <a href="/">Home</a>
      {links.map((l) => (
        <a href={l.href} aria-current={current === l.href ? 'page' : undefined}>{l.label}</a>
      ))}
      <a href="/contact">Contact</a>
    </nav>
  </div>
</header>
<script is:inline>
(function () {
  var hdr = document.getElementById('hdr');
  function onScroll() { hdr.classList.toggle('stuck', window.scrollY > 40); }
  onScroll();
  window.addEventListener('scroll', onScroll, { passive: true });
  var btn = document.getElementById('menuBtn'), panel = document.getElementById('mobilePanel');
  if (btn && panel) {
    btn.addEventListener('click', function () {
      var open = panel.classList.toggle('open');
      btn.setAttribute('aria-expanded', String(open));
      btn.textContent = open ? 'Close' : 'Menu';
      document.documentElement.style.overflow = open ? 'hidden' : '';
    });
  }
})();
</script>
